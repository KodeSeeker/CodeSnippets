/**
Algorithm 
Consider input 
in =[3,2,1,0,13,1,1]
min_avg=0;
avg=0;
min_avg_so_far=0;
int i=0;
count =2;
while(i<arr.length)
{
    sum=(arr[i]+arr[i++]);
    avg=/2; 
    new_Avg=sum+arr[i]/(count++);
    while(new_Avg<avg)
    {
    new_Avg
     sum += arr[i];
     i++;
    }
}

